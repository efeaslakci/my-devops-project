name: Docker Build Test
on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Kodu Cek
        uses: actions/checkout@v4

      - name: Docker Imajini Insa Et
        run: docker build . --file Dockerfile --tag devops-projem:latest

      - name: Basari Mesaji
        run: echo "Docker imajı başarıyla oluşturuldu! 🚀"
